on:
  - push
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v4
      - name: Escribir texto
        run: |
          echo Este texto se escribe al ejecutar el workflow